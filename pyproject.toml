[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "clicker"
version = "0.0.1"
requires-python = ">=3.12"

[project.scripts]
clicker = "clicker.main:main"
